@model IEnumerable<lab03_MVC.Models.CHUDE>
<p class="lead">Chủ đề sách</p>
@foreach (var item in Model)
{
    <a href="@Url.Action("SPTheochude", "BookStore", new { id = item.MaCD})" class="list-group-item" >
    @item.TenChuDe
        </a>
}
